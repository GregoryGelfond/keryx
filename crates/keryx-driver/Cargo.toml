[package]
name = "keryx-driver"
version = "0.1.0"
description = "keryx's transient-solve glue and the backend abstraction: the one-shot lifecycle, the envelope, and the backend trait a solver (themelios-solve) is injected behind."
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[lints]
workspace = true
